generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

model Donator {
	id String @id @default(uuid())
	donationAmount Int @default(0)
	ipAddr String
}